const converttolowercase = (char) => {
    return conversing = char.trim().toLowerCase().split(' ').join('').toString();
}
function capitalizeFirstLetter(string) {
    return string.charAt(0).toUpperCase() + string.slice(1);
}
module.exports = {
    converttolowercase,
    capitalizeFirstLetter
}